# aws-skeleton (opcional)

Este directorio es solo un **esqueleto** para extender el proyecto a AWS manteniendo el mismo patrón:

- `infra` (Terraform AWS: VPC/EKS/RDS/ALB/Secrets)
- `gitops` (manififiestos ArgoCD)
- `app` (imagen GHCR)

Incluye un ejemplo de **tags** comunes para que todos los recursos AWS queden etiquetados.
